<Window x:Class="ClaudeUsageTracker.Views.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Usage"
        Height="270" Width="300"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#1E1E1E"
        BorderBrush="#3C3C3C"
        BorderThickness="1"
        Deactivated="Window_Deactivated">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Claude Usage" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <Button x:Name="SettingsButton" Content="&#9881;" DockPanel.Dock="Right" Width="28" Height="28"
                    Background="Transparent" BorderThickness="0" Foreground="White" FontSize="14"
                    Click="SettingsButton_Click" Cursor="Hand"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock x:Name="FiveHourLabel" Text="5-hour: -" Foreground="#CCCCCC" FontSize="12"/>
            <ProgressBar x:Name="FiveHourBar" Height="8" Margin="0,4,0,0" Minimum="0" Maximum="100"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock x:Name="SevenDayLabel" Text="7-day: -" Foreground="#CCCCCC" FontSize="12"/>
            <ProgressBar x:Name="SevenDayBar" Height="8" Margin="0,4,0,0" Minimum="0" Maximum="100"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock x:Name="SevenDayOpusLabel" Text="7-day Opus: -" Foreground="#CCCCCC" FontSize="12"/>
            <ProgressBar x:Name="SevenDayOpusBar" Height="8" Margin="0,4,0,0" Minimum="0" Maximum="100"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="4" Foreground="#F0A020" FontSize="11" TextWrapping="Wrap" VerticalAlignment="Bottom"/>

        <TextBlock x:Name="LastRefreshedText" Grid.Row="5" Text="" Foreground="#888888" FontSize="10" Margin="0,10,0,0"/>
    </Grid>
</Window>
